#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../htpasswd/lib/htpasswd-bin.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../htpasswd/lib/htpasswd-bin.js" "$@"
  ret=$?
fi
exit $ret
