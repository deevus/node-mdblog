#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../htdigest/lib/htdigest-bin.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../htdigest/lib/htdigest-bin.js" "$@"
  ret=$?
fi
exit $ret
