#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../markdown/bin/md2html.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../markdown/bin/md2html.js" "$@"
  ret=$?
fi
exit $ret
