var pagan = require('../pagan.js');

var arr = new pagan();

for(x = 1; x < 100; x++) {
  arr.push(x);
}

arr.setPage(process.argv[2] || 1);
arr.setIncrement(process.argv[3] || 5);

console.log(arr.values());


